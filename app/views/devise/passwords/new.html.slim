h1.title.is-1.has-text-black.is-3 Forgout your password?
= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  = f.input :email, placeholder: 'Email'
  = f.button :submit, 'Send me reset password instructions', class: 'is-block is-info is-large is-fullwidth'